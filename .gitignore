# dependencies
/node_modules

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store

# env — NEVER commit real keys
.env
.env*.local

# typescript
next-env.d.ts
*.tsbuildinfo

# local preview tooling & test fixtures
.claude/
public/test-photos.json

# vercel local project link
.vercel
.env*
# Anything *ending* in .env too — `.env*` above only catches names that START
# with ".env", so a secrets file called import.env or production.env would
# otherwise be committable.
*.env
